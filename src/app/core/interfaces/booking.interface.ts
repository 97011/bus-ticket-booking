export interface Bookings {
  bookingId: string;
  travelDate: string;
  mobile: string;
  seats: string[];
  boarded: boolean;
}
export interface BookingList {
  bookingId: string
  travelDate: string
  mobile: string
  seats: string[]
  boarded: boolean
}